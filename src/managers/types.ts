import { FlatRoomPosition } from 'types'

/** Memory structure for tracking dropped energy locations */
export interface DroppedEnergyMemory {
    /** Position where energy was dropped */
    pos: FlatRoomPosition
}
